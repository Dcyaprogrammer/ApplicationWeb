# ⚖️ 数值平衡问题报告

生成时间: 2025-05-01

## 🔍 测试发现

### 时间推进：✅ 正常工作

```
测试 1: 9 回合，125 天，平均 13.9 天/回合
测试 2: 10 回合，159 天，平均 15.9 天/回合
测试 3: 8 回合，125 天，平均 15.6 天/回合
```

**结论**: 时间推进修复成功，每回合推进约 15 天 ✅

### 数值平衡：❌ 过于困难

**失败原因统计**：
- 能量（Energy）归零：最常见
- GPA 归零：偶尔发生
- 心态（Mentality）归零：偶尔发生
- 经验（Experience）归零：较少

**失败时间**：
- 通常在 Day 125-159 之间
- 约 8-10 回合后
- 远未达到 Day 300 的胜利条件

## 📊 问题分析

### 1. 数值消耗过快

查看 demoData.ts 中的卡牌效果：

```typescript
// 典型的动态卡
{
  effect: {
    stats: { energy: -15, experience: 5, mentality: -10 },
    // 消耗 25 点，只回复 5 点
  }
}

// 典型的危机卡
{
  effect: {
    stats: { mentality: -30, energy: -30 },
    // 一回合消耗 60 点！
  }
}
```

**问题**：
- 负面效果太强（-15, -20, -30）
- 正面效果太弱（+5, +10）
- 净消耗通常是 -10 到 -20

### 2. 初始属性问题

```typescript
const INITIAL_STATS = {
  gpa: 50,
  mentality: 50,
  energy: 50,
  experience: 50,
};
```

50 点的初始值在面对 -15 到 -30 的消耗时，只能坚持 3-5 个负面选择。

### 3. 缺少恢复机会

虽然游戏中有 Recovery 类型的卡，但：
- 数量太少（只有 2-3 张）
- 触发条件严格（需要特定压力值）
- 冷却时间限制

## 💡 建议的解决方案

### 方案 1: 调整初始值（最快）

```typescript
const INITIAL_STATS = {
  gpa: 75,        // 50 → 75
  mentality: 75,  // 50 → 75
  energy: 75,     // 50 → 75
  experience: 75, // 50 → 75
};
```

**优点**：简单、快速
**缺点**：治标不治本

### 方案 2: 削减负面效果（推荐）

```typescript
// 调整规则：
- 小消耗：-5 → -3
- 中消耗：-15 → -10
- 大消耗：-30 → -20

// 保持正面效果：
+5, +10, +15 不变
```

**优点**：平衡所有卡牌
**缺点**：需要修改很多卡

### 方案 3: 增加恢复机会（推荐）

```typescript
// 添加更多恢复卡
{
  id: "dynamic_weekend_rest",
  effect: {
    stats: { energy: 15, mentality: 10, experience: -5 },
    daysToAdvance: 7,
  }
}

// 降低恢复卡的触发条件
// 减少冷却时间
```

**优点**：增加游戏策略性
**缺点**：需要添加新内容

### 方案 4: 改进 Director 算法（长期）

```typescript
// 在 director.ts 中添加强制恢复逻辑
if (anyStatBelow(25)) {
  // 强制选择恢复卡，不管其他因素
  return forcePickRecoveryCard();
}
```

**优点**：系统性解决
**缺点**：需要较大改动

## 🎯 建议的优先级

### 紧急修复（让游戏能玩通）

1. **提升初始值到 75**（1 分钟）
2. **削减最大消耗到 -20**（10 分钟）

### 短期改进（平衡性）

3. **添加 5-10 张恢复卡**（30 分钟）
4. **调整中等消耗卡**（1 小时）

### 长期优化（完整平衡）

5. **实现 Director 的强制恢复逻辑**
6. **跑模拟器测试 1000 局**
7. **根据数据调整**

## 🧪 测试建议

### 测试目标

- 胜利率：应该达到 30-50%
- 平均时长：40-60 回合
- 平均天数：Day 200-300
- 失败分布：四个属性归零概率相近

### 测试方法

```bash
# 运行 100 次模拟
for i in {1..100}; do
  bun run src/db/testTimeProgression.ts 2>&1 | \
    grep -E "(Victory:|Total turns:)" >> test_results.txt
done

# 分析结果
grep "Victory: ✅" test_results.txt | wc -l  # 胜利次数
grep "Total turns:" test_results.txt | awk '{print $3}' | avg  # 平均回合
```

## 📝 当前状态

✅ **已完成**：
- 时间推进修复
- 所有卡都有 daysToAdvance
- 添加了时间胜利条件

⚠️ **新发现问题**：
- 数值平衡过难
- 需要进一步调整

🎯 **下一步**：
- 确定要采用哪个方案
- 实施平衡调整
- 重新测试验证

---

**这不在当前"时间推进修复"的范围内，但可以作为下一个任务处理。**
